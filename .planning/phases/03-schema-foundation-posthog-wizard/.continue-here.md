---
phase: 03-schema-foundation-posthog-wizard
task: complete
total_tasks: complete
status: complete
last_updated: 2026-04-02T08:03:46.888Z
---

<current_state>
Phase 3 is fully complete and verified. All 2 plans executed, VERIFICATION.md marked passed after
human terminal testing. Phase 4 (Braintrust Adapter) is next — not yet planned or discussed.
</current_state>

<completed_work>

- Phase 3, Plan 01: AmplitudeWatchConfig, BraintrustWatchConfig, LangSmithWatchConfig added to
  schema.py as BaseModel subclasses (not BaseSettings — critical constraint). 164 tests passing.
  Requirements FOUND-01, FOUND-02, FOUND-03 closed.

- Phase 3, Plan 02: PostHog adapter selection added to gateway init wizard via
  questionary.checkbox(). Multiple UX iterations during human testing:
  - Fixed empty-list bug (pre-checked default → unchecked, users select explicitly)
  - Added pre-checkbox instruction: "(Use Space to select adapters, Enter to confirm)"
  - Added confirmation message after selection: "You selected N adapters: X, Y. We'll walk through each one now."
  - Consistent "leave blank to skip" on every adapter's first prompt; skips rest of that adapter
  - 170 tests passing. FOUND-04 closed.

- Verification: human-verified wizard flow in real terminal. All automated checks passed (12/13),
  human test confirmed checkbox UX, confirmation message, skip behavior all correct.

- Phase completion committed: `1e81208`
</completed_work>

<remaining_work>

Nothing remaining in Phase 3.

Phase 4 (Braintrust Adapter) needs:
1. /gsd:discuss-phase 4 or /gsd:plan-phase 4 (no CONTEXT.md or RESEARCH.md yet)
2. Critical: Braintrust HMAC-SHA256 header name is UNKNOWN — must look up docs before planning
3. normalize() must return None when details.is_test == true (first action in normalize)
</remaining_work>

<decisions_made>

- Checkbox defaults to unchecked: users select explicitly, not deselect from pre-checked.
  Rationale: standard checkbox UX expectation; pre-checked caused 3 rounds of confusion in testing.
- "Leave blank to skip" pattern: skips entire adapter section. No separate "skip?" prompt.
  Rationale: keeps wizard fast; escape hatch is visible without doubling question count.
- Arize Phoenix deferred to v0.5.0+: OSS version has no webhooks. Arize AX (hosted/paid) only.
</decisions_made>

<blockers>
None. Phase 3 is clean.
</blockers>

<context>
v0.4 milestone is building out 3 new webhook adapters (Amplitude, Braintrust, LangSmith) + PostHog
gateway init support. The schema foundation is in place. Each adapter follows the same pattern:
verify_signature() → normalize() → PreFilter → Classifier → Writer.

Critical pitfalls to carry forward:
- Braintrust: normalize() must return None when details.is_test == true
- condense() must never include timestamps/IDs (breaks dedup window)
- WatchConfig subclasses must be BaseModel not BaseSettings (v0.2.0 lesson)
- uv auto-loads .env — delete test .env after wizard testing or integration tests get 401
</context>

<next_action>
/clear first, then: /gsd:plan-phase 4
Braintrust HMAC header name is unknown — researcher will need to look it up before planning.
</next_action>
