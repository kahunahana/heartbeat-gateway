---
phase: 01-cli-foundation-gateway-doctor
task: 0
total_tasks: 5
status: ready_to_execute
last_updated: 2026-03-26T04:03:48.166Z
---

<current_state>
Phase 1 is fully planned and verified. No execution has started yet. Both PLAN.md files exist, RESEARCH.md and VALIDATION.md are written, ROADMAP.md is created, and STATE.md is reconstructed. The project is sitting at the start of Wave 1 — ready to run `/gsd:execute-phase 1`.
</current_state>

<completed_work>

**This session (planning session — no code written):**

- Reconstructed STATE.md from PROJECT.md + REQUIREMENTS.md + research artifacts (STATE.md was missing)
- Created ROADMAP.md from REQUIREMENTS.md traceability table + research SUMMARY.md
- Created phase directory: `.planning/phases/01-cli-foundation-gateway-doctor/`
- Spawned gsd-phase-researcher → wrote `01-RESEARCH.md` (HIGH confidence, all 15 req IDs addressed)
- Created `01-VALIDATION.md` (Nyquist validation strategy — all 15 tasks mapped to automated commands, Wave 0 defined)
- Spawned gsd-planner → wrote `01-01-PLAN.md` (Wave 1) and `01-02-PLAN.md` (Wave 2)
- Spawned gsd-plan-checker → VERIFICATION PASSED (one advisory: DOC-07 PostHog secret omitted — architecturally correct since PostHog adapter is out of scope)
- Committed all artifacts

**Nothing executed. No code changed.**

</completed_work>

<remaining_work>

**All execution is remaining:**

- Plan 01-01 (Wave 1, autonomous): Wire Click group in `cli.py`, add 4 explicit deps to `pyproject.toml`, change entry point, create Wave 0 xfail test stubs
- Plan 01-02 (Wave 2, checkpoint gate): Implement DoctorRunner with 9 checks in `commands/doctor.py`, wire doctor to cli, flip xfail test stubs to passing assertions, human-verify terminal output

Also note: `pyproject.toml` and `uv.lock` have a pre-existing uncommitted change (litellm pinned to `<1.82.7` for supply chain audit). This was NOT added this session — it predates the planning work. Should be committed separately before executing.

</remaining_work>

<decisions_made>

- **Continue without CONTEXT.md** — project-level research was HIGH confidence and covered Phase 1 fully; discuss-phase would have been redundant
- **2 plans not 1** — strict build order: cli.py group must exist before doctor can be registered; Wave 1 verifies entry point is unbroken before any check logic is written
- **EXPECTED_MIN_BODY_BYTES replicated in doctor.py** — DOC-09 needs the 512KB constant but cannot import from `app.py`; replication preferred over moving the constant (safer, no risk of touching app.py)
- **DOC-12 `--env-file` uses `load_dotenv(override=True)`** — so the specified file wins over existing env vars (intended behavior for staging/test envs)
- **xfail stub strategy** — Wave 0 stubs use `@pytest.mark.xfail(strict=False)` so full suite stays green immediately; Plan 02 flips them to real assertions
- **doctor NOT wired to cli in Plan 01** — registered only after DoctorRunner.run() is implemented (independently verifiable steps, prevents half-wired state)

</decisions_made>

<blockers>

- None blocking. One pre-existing constraint: litellm pinned to `<1.82.7` (supply chain audit, BerriAI/litellm#24518). Not blocking Phase 1 execution.

</blockers>

<context>
This is a fresh v0.3.0 milestone — no prior execution work. The goal is to close PG-1 (no onboarding wizard) and PG-2 (no doctor command) with two CLI subcommands. Phase 1 = doctor only. Phase 2 = init wizard (not yet planned).

The single highest architectural constraint: `cli.py` and `commands/doctor.py` must never import from `app.py`. The existing entry point (`heartbeat_gateway.app:main`) is being replaced by (`heartbeat_gateway.cli:cli`) with `invoke_without_command=True` so bare `heartbeat-gateway` still starts uvicorn.

The SOUL.md content linter (PG-3) folds into doctor at zero cost — it's check DOC-10 in Plan 02.

Test pattern mandate: `monkeypatch.setenv` only, never mocked `GatewayConfig`. This guards against the v0.2.0 BaseSettings regression where mocked tests passed but real config loading failed silently.
</context>

<next_action>
Start with: `/clear` → `/gsd:execute-phase 1`

Before executing, optionally commit the pre-existing pyproject.toml change (litellm pin) separately:
```
git add pyproject.toml uv.lock && git commit -m "fix: pin litellm <1.82.7 pending supply chain audit resolution"
```
</next_action>
