---
phase: 04-braintrust-adapter
task: 0
total_tasks: 8
status: ready_to_execute
last_updated: 2026-04-02T10:15:46.386Z
---

<current_state>
Phase 4 planning is complete. All three PLAN.md files have been created and verified by the plan checker. No execution has started yet. The next action is to run `/gsd:execute-phase 4` in a fresh context window.
</current_state>

<completed_work>

- Phase 4 directory created: `.planning/phases/04-braintrust-adapter/`
- `04-RESEARCH.md` — Braintrust payload structure confirmed; HMAC header name UNCONFIRMED (documented as pre-build gate in Plan 04-01)
- `04-VALIDATION.md` — Nyquist validation strategy written; Wave 0 requirements listed; manual-only verifications identified (wizard smoke test)
- `04-01-PLAN.md` — Wave 1: HMAC gate checkpoint + test stubs/fixtures + full BraintrustAdapter implementation (BTST-01,02,03,04,07)
- `04-02-PLAN.md` — Wave 2: Route wiring in app.py + NormalizedEvent.source Literal update (BTST-05)
- `04-03-PLAN.md` — Wave 2: gateway init Braintrust wizard section + docs/adapters.md update (BTST-06,07)
- All plans verified by gsd-plan-checker: PASSED
- Committed: `8295a35` — research + validation files
</completed_work>

<remaining_work>

- Execute Plan 04-01 (Wave 1):
  - Task 1 (checkpoint): Confirm Braintrust HMAC header name via live UI or docs — choose option-a (HMAC) or option-b (permanent passthrough)
  - Task 2: Write failing test stubs + fixture JSONs (RED state)
  - Task 3: Implement BraintrustAdapter (verify_signature, normalize, condense) — make tests GREEN
- Execute Plan 04-02 (Wave 2, parallel with 04-03):
  - Task 1: Update NormalizedEvent.source Literal to include "braintrust" + route test stubs
  - Task 2: Register /webhooks/braintrust route in app.py
- Execute Plan 04-03 (Wave 2, parallel with 04-02):
  - Task 1: Add Braintrust section to gateway init wizard
  - Task 2: Update docs/adapters.md
  - Task 3 (checkpoint): Manual wizard smoke test
</remaining_work>

<decisions_made>

- Build order: Braintrust → LangSmith → Amplitude (established in v0.4.0 roadmap)
- HMAC header name is a pre-build gate — do NOT guess; check UI or docs first; if no signing UI exists, implement permanent passthrough identical to AmplitudeAdapter (AMP-01 pattern)
- normalize() must return None for is_test as its FIRST check — before any event type branching
- condense() must never include timestamps/IDs to preserve dedup window integrity
- Plans 04-02 and 04-03 are independent and can run in parallel (no shared files)
- require_signatures guard in app.py should only add "braintrust" if HMAC option chosen (not passthrough)
- BraintrustWatchConfig already exists in schema.py from Phase 3 — do not recreate
</decisions_made>

<blockers>

- **HMAC header name (BTST-01):** Braintrust announced HMAC support in March 2026 release notes but the header name is not in public docs as of 2026-04-01. Plan 04-01 Task 1 is a checkpoint:decision to resolve this before writing a single line of verify_signature. Two pre-specified options: (a) HMAC with confirmed header, (b) permanent passthrough with IP allowlisting docstring.
- **litellm pinned to <1.82.7** — carry-forward from v0.2.0 supply chain audit; do not upgrade during this phase.
</blockers>

<context>
Phase 3 (Schema + PostHog wizard) is fully done — 170 tests passing, ruff clean. Phase 4 is the first adapter implementation in v0.4.0. The adapter pattern is well-established from PostHog and GitHub adapters; the planner was instructed to follow it exactly. The HMAC gate is the only genuine uncertainty — everything else (payload shapes, is_test check, wizard pattern, route registration) is high-confidence.

The plan checker noted a minor doc inconsistency: VALIDATION.md references `tests/test_routes.py` for BTST-05 but the actual plans use `tests/test_app.py`. This is informational only — the plans are internally consistent and correct.
</context>

<next_action>
Start with: `/clear` → then `/gsd:execute-phase 4` — Phase 4 is fully planned and ready. Plan 04-01 Task 1 will immediately present the HMAC gate checkpoint.
</next_action>
