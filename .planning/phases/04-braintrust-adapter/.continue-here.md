---
phase: 04-braintrust-adapter
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-04-03T02:44:45.108Z
---

<current_state>
Phase 4 Plan 04-03 (Init Wizard + Docs) is at Task 3 — a `checkpoint:human-verify` that requires the user to run `uv run gateway init` interactively and confirm the Braintrust wizard section works correctly.

Plans 04-01 and 04-02 are fully complete. Plan 04-03 Tasks 1 and 2 are done. A bug was found during the first smoke test attempt (all adapter choices had `checked=False`, causing silent empty-list failure) and was fixed in commit `10fb706`. The fix restored `checked=True` for all 4 adapters. The user had not yet re-run the smoke test before pausing.

The session ended mid-checkpoint — the user has not yet typed "approved".
</current_state>

<completed_work>

- Plan 04-01: BraintrustAdapter core — COMPLETE (commits: 977cd6e, d675900, ba77647)
  - verify_signature: permanent passthrough (option-b, Braintrust has no signing)
  - normalize: is_test guard as first line, BTST-02 enforced
  - condense: uses automation name, never timestamps/IDs
  - 11 tests, 3 fixture JSONs created
  - 181 tests total after plan

- Plan 04-02: Route wiring — COMPLETE (commits: 31f7fc8, b13dd8a, a0f7d41)
  - NormalizedEvent.source Literal updated to include "braintrust"
  - POST /webhooks/braintrust route registered in app.py
  - POST /webhook/braintrust 308 redirect added
  - braintrust excluded from require_signatures guard with option-b comment
  - 4 integration tests in test_app.py
  - 185 tests total after plan

- Plan 04-03 Task 1: Braintrust section added to gateway init wizard — DONE (commits: 162fb33, 9758ab7, bacc71e)
  - BTQL instructions displayed inline when Braintrust selected
  - Password prompt for signing secret (blank = skip)
  - Written to .env as GATEWAY_WATCH__BRAINTRUST__SECRET
  - 2 new Braintrust init tests added

- Plan 04-03 Task 2: docs/adapters.md updated with Braintrust documentation — DONE (commit: 9302577)

- Bug fix (during Task 3 smoke test): Restored checked=True for all 4 adapter choices — DONE (commit: 10fb706)
  - Root cause: plan 04-03 implemented adapters with checked=False, violating Phase 3 architectural decision
  - Fix: questionary.Choice("Braintrust", checked=True) and same for PostHog, Linear, GitHub

- 187 tests passing, 1 xfailed, ruff clean after bug fix
</completed_work>

<remaining_work>

- Plan 04-03 Task 3: User re-runs `uv run gateway init` smoke test with fixed code
  - All adapters now start pre-checked; user deselects everything except Braintrust
  - Verify BTQL instructions appear, signing secret prompt appears, .env written correctly
  - Type "approved" → agent completes SUMMARY.md + STATE.md + ROADMAP.md updates

- Phase 4 verification: After 04-03 completes, gsd-verifier runs against BTST-01 through BTST-07
- Phase 4 roadmap update: Mark phase complete, advance STATE.md to Phase 5
</remaining_work>

<decisions_made>

- Braintrust verify_signature: permanent passthrough (option-b) — confirmed via braintrust.dev/docs and changelog; no webhook signing exists
- checked=True restored for all adapter choices — regression from plan 04-03 spec carrying checked=False; the Phase 3 architectural decision is now re-enforced
- Plans 04-02 and 04-03 were executed sequentially (not parallel) due to tool constraints, but are fully independent (no shared files)
</decisions_made>

<blockers>

- None blocking execution. The only open item is the human-verify smoke test for 04-03.
</blockers>

<context>
Phase 4 is 95% done. The adapter itself is complete and wired. The only outstanding item is the user confirming the wizard works interactively after the checked=False bug fix. Once "approved", the agent just needs to write SUMMARY.md and update STATE.md + ROADMAP.md, then the verifier runs.

The agent that was waiting for "approved" (ID: a30266554b9daa644) should be treated as abandoned — spawn a fresh continuation agent using the checkpoint pattern when resuming.
</context>

<next_action>
Start with: Run `uv run gateway init` in C:/Users/chadk/projects/heartbeat-gateway. Deselect all adapters except Braintrust (all start pre-checked now — Space to deselect PostHog, Linear, GitHub). Verify BTQL instructions and signing secret prompt appear. Type "approved" to complete the checkpoint.

Then spawn a fresh continuation agent for 04-03 Task 3 with user_response="approved" to write SUMMARY.md and finalize STATE.md/ROADMAP.md.
</next_action>
